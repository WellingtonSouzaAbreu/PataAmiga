import { StyleSheet } from 'react-native'

const styles = {
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        padding: 10
    },

    txtTitle: {
        fontSize: 19,
        color: 'grey',
        fontWeight: '600',
        textAlign: 'center'
    },

    txtText: {
        fontSize: 17,
        color: 'gray',
        textAlign: 'center',
        marginVertical: 10
    }
}

export default styles