const {request, app}= require('./mainTests.js')

describe('Testing api/visit.js', () => {
    test('Should return empty object and statusCode=204 | | route: POST /veterinary-care', async () => {
        expect(0).toEqual(0)
    })
})